diff --git a/Utilities/cmlibuv/src/unix/linux.c b/Utilities/cmlibuv/src/unix/linux.c
index 0ba2e987a36db38dad8e1575eccf15b757d25320..9bc5d05153e4fc2994a2c76420069106aa33bdc8 100644
--- a/Utilities/cmlibuv/src/unix/linux.c
+++ b/Utilities/cmlibuv/src/unix/linux.c
@@ -124,6 +124,14 @@
 # endif
 #endif /* __NR_getrandom */
 
+/* This is rather annoying: CLOCK_BOOTTIME lives in <linux/time.h> but we can't
+ * include that file because it conflicts with <time.h>. We'll just have to
+ * define it ourselves.
+ */
+#ifndef CLOCK_BOOTTIME
+# define CLOCK_BOOTTIME 7
+#endif
+
 enum {
   UV__IORING_SETUP_SQPOLL = 2u,
   UV__IORING_SETUP_NO_SQARRAY = 0x10000u,
diff --git a/Utilities/cmlibuv/src/unix/udp.c b/Utilities/cmlibuv/src/unix/udp.c
index 029f27f7a9f3e077e189b98ccb2b9b3f74eac5ad..9af0197dfdf70cae643fe53041c1f9cdaea2033b 100644
--- a/Utilities/cmlibuv/src/unix/udp.c
+++ b/Utilities/cmlibuv/src/unix/udp.c
@@ -1410,7 +1410,9 @@ static int uv__udp_sendmsgv(int fd,
   r = 0;
   nsent = 0;
 
-#if defined(__linux__) || defined(__FreeBSD__) || defined(__APPLE__) || \
+#if (defined(__linux__) && \
+  (!defined(__GLIBC__) || (__GLIBC__ >= 2 && __GLIBC_MINOR__ >= 14))) || \
+  defined(__FreeBSD__) || defined(__APPLE__) || \
   (defined(__sun__) && defined(MSG_WAITFORONE)) || defined(__QNX__)
   if (count > 1) {
     for (i = 0; i < count; /*empty*/) {
@@ -1438,7 +1440,9 @@ static int uv__udp_sendmsgv(int fd,
 
     goto exit;
   }
-#endif  /* defined(__linux__) || defined(__FreeBSD__) || defined(__APPLE__) ||
+#endif  /* defined(__linux__) &&
+	 * (!defined(__GLIBC__) || (__GLIBC__ >= 2 && __GLIBC_MINOR__ >= 14))) ||
+	 * defined(__FreeBSD__) || defined(__APPLE__) ||
 	 * (defined(__sun__) && defined(MSG_WAITFORONE)) || defined(__QNX__)
 	 */
 
